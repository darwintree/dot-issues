{
  "name": "dot-issues",
  "version": "0.1.0",
  "description": "Issues that live in your repo — plain Markdown, built for agents.",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "new": "bun src/index.ts new",
    "list": "bun src/index.ts list",
    "modify": "bun src/index.ts modify-metadata"
  },
  "devDependencies": {
    "bun-types": "latest",
    "typescript": "^6.0.2"
  }
}
